{
  "name": "@uphold/process-manager",
  "version": "0.0.0",
  "description": "A module for handling the lifecycle of a node process",
  "keywords": [
    "graceful",
    "manager",
    "process",
    "shutdown"
  ],
  "homepage": "https://github.com/uphold/process-manager",
  "bugs": {
    "url": "https://github.com/uphold/process-manager/issues"
  },
  "license": "MIT",
  "author": "Uphold",
  "main": "index.js",
  "repository": "https://github.com/uphold/process-manager.git",
  "scripts": {
    "changelog": "github-changelog-generator --owner=uphold --repo=process-manager --future-release=$npm_package_version > CHANGELOG.md",
    "lint": "node_modules/.bin/eslint src test",
    "test": "node --harmony node_modules/.bin/jest --coverage --runInBand --verbose",
    "version": "npm run changelog && git add -A CHANGELOG.md"
  },
  "dependencies": {
    "co": "4.6.0",
    "debugnyan": "1.0.0",
    "lodash": "4.17.4"
  },
  "devDependencies": {
    "eslint": "^3.12.2",
    "eslint-config-seegno": "^8.0.1",
    "github-changelog-generator": "git+ssh://git@github.com/uphold/github-changelog-generator#v0.3.0",
    "jest": "^18.0.0",
    "pre-commit": "^1.2.2"
  },
  "engines": {
    "node": ">=6"
  },
  "pre-commit": {
    "run": [
      "lint"
    ]
  }
}
